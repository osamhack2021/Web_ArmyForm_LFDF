import routeConstants from "shared/constants/routes";
import Test from ".";

const route = {
  exact: true,
  path: routeConstants.MAIN.subroutes.TEST.route,
  component: Test,
};

export default route;
